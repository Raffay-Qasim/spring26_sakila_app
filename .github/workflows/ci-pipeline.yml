name: Sakila CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install Linting Tools
        run: pip install flake8
      - name: Run Flake8
        run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: admin
          MYSQL_DATABASE: sakila
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    steps:
      - uses: actions/checkout@v4
      - name: Install MySQL Client
        run: sudo apt-get install -y mysql-client
      - name: Download and Import Sakila DB
        run: |
          curl -L https://downloads.mysql.com/docs/sakila-db.tar.gz | tar xz
          # Sleep to ensure MySQL internal init is finished
          sleep 30
          # Force TCP and use 127.0.0.1 to avoid socket errors
          mysql --protocol=tcp -h 127.0.0.1 -u root -padmin sakila < sakila-db/sakila-schema.sql
          mysql --protocol=tcp -h 127.0.0.1 -u root -padmin sakila < sakila-db/sakila-data.sql
      - name: Run Tests
        env:
          MYSQL_HOST: 127.0.0.1
          MYSQL_USER: root
          MYSQL_PASSWORD: admin
          MYSQL_DB: sakila
          PYTHONPATH: .
        run: |
          pip install -r requirements.txt pytest
          pytest

  build-and-scan:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Docker Image
        run: docker build -t sakila-app:latest .
        
      - name: Smoke Test Image
        run: docker run --rm sakila-app:latest python -c "print('App Image Boots Successfully')"

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'sakila-app:latest'
          format: 'table'
          output: 'trivy-report.txt'
          exit-code: 0
          severity: 'CRITICAL,HIGH'
          
      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-report.txt